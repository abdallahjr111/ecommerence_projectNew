# This file is kept for reference only.
# Active configuration is driven by application.properties + profile-specific files.
# Set spring.profiles.active=mysql in application.properties to use MySQL with Flyway.
# Set spring.profiles.active=h2  in application.properties to use H2 in-memory (default).
